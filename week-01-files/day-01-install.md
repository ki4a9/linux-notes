День 1. 
- Поставил Ubuntu Server в VM. (ubuntu-26.04-live-server)
- Настроил сеть и прокси для apt 
- Обновил систему. 
- Создал пользователя, дал sudo. 

В современных дистрибутивах убунту сеть настраивается по другому. В файле /etc/netplan/*.yaml
В моем дистрибутиве не оказалось установлено ни одного текстового редактора поэтому пришлось править файлы настроек через cat sudo tee /etc/netplan/*.yaml > /dev/null << 'EOF'

